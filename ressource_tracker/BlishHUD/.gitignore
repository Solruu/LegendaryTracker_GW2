# Sorties de build et cache Visual Studio
bin/
obj/
.vs/
*.user
# Exception assumee : depot mono-poste, ce fichier porte le lancement externe
# de Blish HUD et les arguments de debogage. Le perdre casse F5.
!GW2_NodeTracker.csproj.user
*.suo

# Paquets NuGet : restaures par Visual Studio depuis packages.config
packages/

# Binaires copies a la racine du projet par la restauration NuGet
# (MonoGame / SharpDX / BlishHUD). Regeneres au build.
# Si un clone neuf ne compile pas, c'est cette section qu'il faut retirer.
CppNet.dll
libmojoshader_64.dll
mgfxc.*
SharpDX*.dll
SharpDX*.xml
# Contenu BlishHUD (.xnb) copie par BlishHUD.targets
Content/

# Module compile
*.bhm
